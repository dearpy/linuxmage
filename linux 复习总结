LINUX PORTS
lvs：
keepalived：
haproxy： 5000
nginx：

varnish：
memcache：
redis：

http：80  https 443
    httpd是Apache超文本传输协议(HTTP)服务器的主程序。被设计为一个独立运行的后台进程，它会建立一个处理请求的子进程或线程的池。
    程序包：httpd
    配置文件：/etc/httpd
    主程序：
    状态码：
    一次完整的http请求过程：
    http首部信息：
    主要可调参数：
php-fpm： 9000
tomcat：
php：
mysql：3306
java：
ssh：22
ftp：21 20
telnet：23
smtp：25
bind： 53 dns
vsftp：
nfs：
samba：

mogilefs：7001
mogstored：接收客户端请求 7500 为tracer提供健康检测及管理端口 7501

pxe：
    cobber：
        dhcp tftp ftp/http
ansible：
puppet：agent 8139 master 8140
elk：
    elasticsearch：9200 接收客户端请求 9300 集群内通信
    kibana：
    logstash：
zabbix：
git：
iptables：

kvm：
hadoop：
openstack：
docker：
